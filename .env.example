OPENAI_API_KEY=sk-...
XAI_API_KEY=xai-...
OPENAI_MODEL=gpt-4o
XAI_MODEL=grok-2-1212

CHROMA_PATH=./data/chroma
SQLITE_PATH=./data/bayes.db
RAG_TOP_K=5

NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=drholmes123

REDIS_URL=redis://localhost:6379

# Postgres (optional — falls back to SQLite at ./data/cases.db if unset)
# DATABASE_URL=postgresql://drholmes:drholmes@localhost:5432/drholmes

# API server
DR_HOLMES_AUTH_MODE=dev          # dev | jwt
# DR_HOLMES_JWT_SECRET=...        # only required when AUTH_MODE=jwt
MAX_CONCURRENT_CASES=5
